package model.player.buildings.edge

import model.player.buildings.Buildable

/**
 * Interface that represents an edge structure. It can only be placed on the empty edges of the
 * hexagons of the board.
 */
trait Edge extends Buildable {

}
